<% if @order.errors.any? || @order_item.errors.any? %>
alert("not valid.")
<% else %>
alert("added product")
<%# document.getElementById('cart-text').innerHtml = "<%= escape_javascript(render 'layouts/cart_text') %>" %>
$(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>")
<% end %>